# Backend (PocketBase)

This directory is reserved for PocketBase migrations, hooks, schema definitions, and backend scripts.

## Planned Setup
- [PocketBase](https://pocketbase.io/) standalone executable or Docker container.
- Note: The database data directory `pb_data/` is gitignored.
